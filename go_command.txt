go build main.go //สร้างไฟล์ compile มาก่อนแล้วค่อยเอาไฟล์ ./main ไปรัน


npm install -g nodemon //ติดตั้ง nodemon

go mod init github.com/DEAR/go-example //นำ GO module มาใช้

go get github.com/google/uuid //ลง libary generate uuid มาทดลอง


go list -m all //ตรวจสอบ libary ว่ามี package อะไรบ้าง


ถ้า func เป็นชื่อตัวพิมพ์เล็กจะ import ไม่ได้


